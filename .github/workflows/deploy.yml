name: Demo Workflow
on: workflow_dispatch

jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print chtochto
        run: sudo apt install traceroute 
      - name: tc
        run: traceroute 1.1.1.1

